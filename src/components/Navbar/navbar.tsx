import React from "react";
import { Theme } from "../../assets/svg";
import "./_navbar.scss";

const Navbar = () => {
    return (
        <nav className="navbar">
            <Theme />
        </nav>
    );
};

export default Navbar;
